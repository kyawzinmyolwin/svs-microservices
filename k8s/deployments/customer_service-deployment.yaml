apiVersion: apps/v1
kind: Deployment
metadata:
  name: svs-app-customer
  namespace: svs-microservices
spec:
  replicas: 2
  selector:
    matchLabels:
      app: svs-app-customer
  template:
    metadata:
      labels:
        app: svs-app-customer
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "svs-role"
        vault.hashicorp.com/agent-inject-secret-db-creds: "secret/data/svs/db"
        vault.hashicorp.com/agent-inject-template-db-creds: |
          {{- with secret "secret/data/svs/db" -}}
          DB_USERNAME={{ .Data.data.username }}
          DB_PASSWORD={{ .Data.data.password }}
          {{- end }}        
    spec:
      serviceAccountName: svs-app-sa
      containers:
        - name: svs-app
          image: piconet/svs_microservices-customer_service:1.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 5002
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          envFrom:
            # - secretRef:
            #     name: customer-db-secret
            - configMapRef:
                name: customer-config              